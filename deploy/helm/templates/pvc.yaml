apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "walacor-gateway.fullname" . }}-wal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.walVolume.size }}
